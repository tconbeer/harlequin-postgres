services:
 
  db:
    image: postgres:17
    restart: always
    environment:
      POSTGRES_PASSWORD: for-testing
    volumes:
        - pgdata:/var/lib/postgresql/data
    ports:
        - 5432:5432
 
volumes:
  pgdata:
